const reveal=new IntersectionObserver((entries)=>entries.forEach(e=>{if(e.isIntersecting){e.target.style.opacity=1;e.target.style.transform='translateY(0)';reveal.unobserve(e.target)}}),{threshold:.12});document.querySelectorAll('.intro,.destinations,.experience').forEach(el=>{el.style.opacity=0;el.style.transform='translateY(25px)';el.style.transition='opacity .8s ease,transform .8s ease';reveal.observe(el)});
const packageField=document.getElementById('package');document.querySelectorAll('.book-btn').forEach(b=>b.addEventListener('click',()=>{packageField.value=b.dataset.package;document.getElementById('booking').scrollIntoView({behavior:'smooth'})}));document.getElementById('bookingForm').addEventListener('submit',e=>{e.preventDefault();const origin=document.getElementById('origin').value;document.getElementById('status').textContent='Permintaan booking diterima dari '+origin+'. Admin akan menghubungi kamu untuk konfirmasi detail perjalanan.';e.target.reset();});